# Spotify OAuth Configuration
# IMPORTANT: Never commit actual secrets to version control

# Client ID - Safe to expose in frontend
VITE_CLIENT_ID=your_spotify_client_id_here

# Client Secret - MUST be kept on backend only, never in frontend
# Do NOT prefix with VITE_ as it exposes to client-side
CLIENT_SECRET=your_spotify_client_secret_here

# Application URLs
VITE_REDIRECT_URI=http://localhost:5173/callback
VITE_BACKEND_URL=http://localhost:3001

# Optional: Database connection for session storage
DATABASE_URL=your_database_connection_string

# Session Secret for JWT signing
SESSION_SECRET=your_random_session_secret_here
