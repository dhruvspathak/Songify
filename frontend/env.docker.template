# Songify Frontend Configuration for Docker/Podman
# Copy this file to .env when using containerized deployment
# IMPORTANT: Never commit .env to version control

# Frontend Environment Variables (safe to expose - for containerized setup)
VITE_CLIENT_ID=your_spotify_client_id_here
VITE_REDIRECT_URI=http://localhost:8080/callback
VITE_BACKEND_URL=http://localhost:3000
